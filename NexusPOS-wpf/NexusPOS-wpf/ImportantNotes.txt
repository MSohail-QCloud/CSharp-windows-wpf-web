MessageBox.Show("Do you want to delete Bill and all transactions ?","Confirm Delete", MessageBoxButton.YesNo)==MessageBoxResult.Yes)

//for integer type validation on textbox
private void txtQty_PreviewTextInput(object sender, TextCompositionEventArgs e)
        {
            Regex regex = new Regex("[^0-9]+");
            e.Handled = regex.IsMatch(e.Text);
        }


//for point type validation on textbox
private void txtRate_PreviewTextInput(object sender, TextCompositionEventArgs e)
        {
            var regex = new Regex(@"^[0-9]*(?:\.[0-9]*)?$");
            if (regex.IsMatch(e.Text) && !(e.Text == "." && ((TextBox)sender).Text.Contains(e.Text)))
                e.Handled = false;
            else
                e.Handled = true;
        }